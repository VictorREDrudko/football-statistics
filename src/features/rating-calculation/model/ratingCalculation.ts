

import { calculationPoints } from '../lib/calculationPoints';


export const ratingCalculation = (teamsStats: string) => {
  // Object.keys(nationalTeamsData).forEach((nameCountry) => {
  //   nationalTeamsData[nameCountry].rating.points = calculationPoints(nameCountry, teamsStats);
  // })

  // const nationalTeams = {...nationalTeamsData};
  // const sortByRatingPoints = Object.keys(nationalTeams).sort((a, b) => {
  //   return nationalTeams[b].rating.points - nationalTeams[a].rating.points
  // })

  // console.log(sortByRatingPoints)

  // Object.keys(nationalTeamsData).forEach((nameCountry) => {
  //   nationalTeamsData[nameCountry].rating.place = sortByRatingPoints.findIndex((country) => country === nameCountry) + 1;
  // })
}

